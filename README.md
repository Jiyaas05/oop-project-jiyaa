This simulator lets a user enter a basic Boolean expression and then generates the full truth table for it.
It evaluates the expression using the variables A, B, and C and shows how the result changes across all possible combinations. 
The program runs in a loop so the user can try different expressions or exit when they’re finished. 
